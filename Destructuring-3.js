/*Given a color array with three elements, 
use array destructuring to create variables 
firstColor, secondColor and < a i=3>which 
contain the values ​​of the array.ThirdColor*/

const colors = ["red", "green", "blue"];

const [firstColor, secondColor, thirdColor] = colors;

console.log(secondColor,firstColor,  thirdColor);